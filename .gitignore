# Android / Gradle
*.iml
.gradle/
/build/
app/build/
local.properties
*.log

# Agent workspace (pamięć / notatki robocze — lokalnie tylko)
.workbuddy-ai/

# Native build intermediates
app/.cxx/
.externalNativeBuild/
cmake-build-*/

# IDE
.idea/
*.jks
*.keystore
captures/

# OS
.DS_Store
Thumbs.db

# Build logs (local only)
build_log*.txt
build_log_rel.json
crash_log*.txt
app-debug-v2.apk
app-debug-v1.apk

# Agent helper scripts / temp JSON (not part of app source)
*.js
firestore_*.json
ui_dump.xml

# Vendored llama.cpp master (separate repo, do not embed)
llama_master/

# Model binaries (large, fetched separately)
*.gguf
model_probe/
verbigem-host-toolchain.cmake

# Hermes composer images
AppData/
composer-images/

# Vendored llama.cpp (pobierany przez scripts/fetch_llama.sh)
app/src/main/cpp/llama.cpp/

# Firebase config (rules contain schema/security detail — keep local only, not in public repo)
firestore.rules
firebase.json

# Firebase CLI cache
.firebase/

# Kotlin incremental-compilation caches (Gradle 9 / KGP 2.x)
.kotlin/
